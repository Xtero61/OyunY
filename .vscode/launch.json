{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lua-local",
            "request": "launch",
            "name": "Debug File",
            "program": {
                "command": "/usr/bin/love"
            },
            "cwd": "${workspaceFolder}",
            "args": [ "${fileDirname}" ]
        }
    ]
}